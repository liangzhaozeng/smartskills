apiVersion: v1
kind: Service
metadata:
  name: skills-directory
  namespace: skills-directory
  labels:
    app.kubernetes.io/name: skills-directory
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: skills-directory
